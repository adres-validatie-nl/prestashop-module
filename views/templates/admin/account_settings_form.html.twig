{{ form_start(accountSettingsForm) }}
<div class="card">
    <h3 class="card-header">
        <i class="material-icons">settings</i> {{ 'Account credentials'|trans({}, 'Modules.AdresValidatie.Admin') }}
    </h3>
    <div class="card-body">
        <div class="form-wrapper">
            {{ form_widget(accountSettingsForm) }}
        </div>
    </div>
    <div class="card-footer">
        <div class="d-flex justify-content-end">
            <button class="btn btn-primary float-right" id="save-button">
                {{ 'Update'|trans({}, 'Admin.Actions') }}
            </button>
        </div>
    </div>
</div>
{{ form_end(accountSettingsForm) }}
